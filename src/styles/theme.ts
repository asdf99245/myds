import { COLORS } from './colors';
import { FONTS } from './fonts';
import { SPACING } from './spacing';

const theme = {
  colors: COLORS,
  spacing: SPACING,
  fonts: FONTS,
};

export default theme;
